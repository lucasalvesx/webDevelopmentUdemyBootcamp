//to start jquery we use the $ sign

//to make sure that jquery waits until the browser reads it so it could be able to start,
//we declare the ready function

$(document).ready(function(){
    $("h1").css("color", "red");
})
